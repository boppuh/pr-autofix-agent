validate:
  - name: lint
    run: ruff check .
  - name: typecheck
    run: mypy pr_agent
  - name: test
    run: pytest -x

exclude_paths:
  - ".venv/**"
  - "**/__pycache__/**"

max_files_per_patch: 5

bugbot_logins:
  - "cursor[bot]"
